{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "get_credentials_status",
  "description": "Get the current credential configuration status for the MCP session. Returns whether credentials are configured, the environment (testnet/mainnet), API key prefix (first 8 characters), and configuration timestamp. This tool requires no parameters.",
  "type": "object",
  "properties": {},
  "additionalProperties": false,
  "success_response": {
    "oneOf": [
      {
        "description": "Credentials not configured",
        "type": "object",
        "properties": {
          "configured": {
            "type": "boolean",
            "const": false
          }
        },
        "required": ["configured"]
      },
      {
        "description": "Credentials configured",
        "type": "object",
        "properties": {
          "configured": {
            "type": "boolean",
            "const": true
          },
          "environment": {
            "type": "string",
            "enum": ["testnet", "mainnet"],
            "description": "Binance environment currently configured"
          },
          "key_prefix": {
            "type": "string",
            "minLength": 8,
            "maxLength": 8,
            "description": "First 8 characters of API key (security by obscurity per NFR-003)"
          },
          "configured_at": {
            "type": "string",
            "format": "date-time",
            "description": "ISO8601 timestamp when credentials were configured"
          }
        },
        "required": ["configured", "environment", "key_prefix", "configured_at"]
      }
    ]
  },
  "error_responses": [],
  "examples": [
    {
      "input": {},
      "output_not_configured": {
        "configured": false
      },
      "output_configured": {
        "configured": true,
        "environment": "testnet",
        "key_prefix": "12345678",
        "configured_at": "2025-10-19T10:30:45Z"
      }
    }
  ],
  "notes": [
    "Tool requires no parameters (empty object)",
    "Never returns full API key or secret (security requirement NFR-002, NFR-003)",
    "Fast response time <50ms per NFR-004 (in-memory lookup)",
    "Useful for users to verify which environment they're using before trading"
  ]
}
